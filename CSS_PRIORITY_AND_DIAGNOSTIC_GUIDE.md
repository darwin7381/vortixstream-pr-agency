# CSS 優先級與診斷指南

## 📊 CSS 優先級完整解析

### 1. 基礎優先級規則（永遠不變）

#### 選擇器優先級（Specificity）
```
!important            = 10,000  ⬅️ 最高（避免使用）
內聯 style=""         = 1,000
#id                   = 100
.class, [attr]        = 10
tag                   = 1
```

**範例：**
```css
/* 優先級 = 1 */
div { background: red; }

/* 優先級 = 10 */
.bg-black { background: black; }

/* 優先級 = 100 */
#hero { background: blue; }

/* 優先級 = 1,000 */
<div style="background: white;">  ⬅️ 最終顯示白色
```

**重要：編譯方式不影響優先級！**
```
預編譯的 .bg-black { background: #000; }  優先級 = 10
動態生成的 .bg-black { background: #000; }  優先級 = 10
→ 完全相同！
```

---

### 2. Tailwind CSS 的 @layer 系統

#### @layer 優先級順序
```css
@layer base {
  /* 優先級最低 */
  body { background: white; }
}

@layer components {
  /* 優先級中等 */
  .btn { padding: 1rem; }
}

@layer utilities {
  /* 優先級最高（在 class 中） */
  .bg-black { background: black !important; }  ⬅️ 會覆蓋 base
}
```

**實際效果：**
```html
<body class="bg-black">
  <!-- 
    @layer base 的 body { background: white; } 
    被
    @layer utilities 的 .bg-black { background: black; }
    覆蓋
    
    最終顯示：黑色 ✅
  -->
</body>
```

---

### 3. 完整優先級層級圖

```
最高 ↑
│
├── 內聯 style=""                     (1,000)
│   └── <div style="background: red;">  ⬅️ 覆蓋所有 class
│
├── @layer utilities                   (10+)
│   └── .bg-black, .text-white, etc.   ⬅️ Tailwind 工具類別
│
├── @layer components                  (10)
│   └── .btn, .card, etc.             ⬅️ 自定義組件
│
├── @layer base                        (1-10)
│   └── body, h1, etc.                ⬅️ 全域基礎樣式
│
最低 ↓
```

**特殊規則：**
```
同一層級內：
└── 後面定義的覆蓋前面的（CSS 源順序）

跨層級：
└── utilities > components > base（無視源順序）
```

---

### 4. 內聯樣式的研究結果

#### 結論 1：內聯永遠最高
```tsx
<div className="bg-black text-white" style={{ background: 'blue', color: 'red' }}>
  最終樣式：
  ✓ background: blue（內聯覆蓋 .bg-black）
  ✓ color: red（內聯覆蓋 .text-white）
</div>
```

**無論 CSS 怎麼設定，內聯永遠贏！**

#### 結論 2：內聯不影響其他樣式
```tsx
<div className="bg-black p-4 rounded-lg" style={{ background: 'blue' }}>
  最終樣式：
  ✓ background: blue（內聯）
  ✓ padding: 1rem（.p-4 正常工作）
  ✓ border-radius: 0.5rem（.rounded-lg 正常工作）
</div>
```

**內聯只覆蓋它設定的屬性，不影響其他！**

#### 結論 3：動態內聯必須保留
```tsx
<div 
  style={{
    left: `${Math.random() * 100}%`,  ⬅️ 動態值，無法用 CSS
    opacity: Math.random() * 0.3      ⬅️ 動態值，必須保留
  }}
>
```

**如果是動態計算的值（Math.random(), props, state），必須用內聯！**

#### 結論 4：靜態內聯可以移除
```tsx
/* ❌ 不好：靜態值用內聯 */
<div style={{ background: '#000', color: '#fff' }}>

/* ✅ 好：用 CSS class */
<div className="bg-black text-white">
```

**靜態值應該改用 CSS class，更可維護！**

---

### 5. 衝突檢測與處理

#### 步驟 1：識別內聯樣式類型

**A. 動態內聯（保留）**
```tsx
style={{
  left: `${position}%`,           ⬅️ 動態，保留 ✅
  opacity: Math.random(),         ⬅️ 動態，保留 ✅
  animation: `slide ${duration}s` ⬅️ 動態，保留 ✅
}}
```

**B. 靜態內聯（可能需要處理）**
```tsx
style={{
  background: '#000',              ⬅️ 靜態，考慮改用 class
  color: '#fff',                   ⬅️ 靜態，考慮改用 class
  padding: '20px'                  ⬅️ 靜態，考慮改用 class
}}
```

**C. 複雜計算（保留）**
```tsx
style={{
  background: `
    radial-gradient(circle at 20% 30%, rgba(139, 92, 246, 0.15) 0%, transparent 50%),
    linear-gradient(135deg, #000000 0%, #0a0a0f 50%, #000000 100%)
  `
}}
```
**複雜的漸層和計算，保留內聯更清晰 ✅**

#### 步驟 2：檢查是否與 CSS 衝突

**檢查方法：**
```tsx
// 1. 查看元素
<div className="bg-black" style={{ background: 'white' }}>

// 2. 檢查 CSS
.bg-black { background: black; }  /* 被內聯覆蓋，無效 */

// 3. 判斷
衝突！內聯的 white 覆蓋了 CSS 的 black
→ 決定保留哪個？
```

#### 步驟 3：處理策略

**策略 A：CSS 優先（推薦）**
```tsx
// 移除內聯，改用 CSS class
<div className="bg-black">  ✅

// CSS 中確保有定義
.bg-black { background: black; }  ✅
```

**策略 B：內聯優先**
```tsx
// 保留內聯，移除 CSS class
<div style={{ background: 'white' }}>  ✅

// 或保留內聯，CSS 當備用
<div className="bg-black" style={{ background: 'white' }}>
// 最終顯示 white（內聯生效）
```

---

## 🔍 樣式跑掉的診斷流程

### 當樣式不正確時，按順序檢查：

#### 檢查 1：CSS 類別是否定義？
```bash
# 例如：py-section-large 不work
grep "\.py-section-large" src/index.css
grep "\.py-section-large" src/styles/utilities.css

# 如果找不到 → 問題：CSS 中缺少定義！
# 解決：從 backup 提取並添加
```

#### 檢查 2：CSS 變數是否存在？
```bash
# 例如：--spacing-section-large-mobile
grep "spacing-section-large-mobile" src/styles/variables.css

# 如果找不到 → 問題：變數缺失！
# 解決：從 backup 提取變數
```

#### 檢查 3：@layer 順序是否正確？
```css
/* 正確順序 */
@layer base { ... }       /* 第一層 */
@layer components { ... } /* 第二層 */
@layer utilities { ... }  /* 第三層 */

/* 如果順序錯誤，utilities 可能被 base 覆蓋 */
```

#### 檢查 4：是否有內聯樣式覆蓋？
```tsx
// 使用瀏覽器開發者工具
// 1. 檢查元素
// 2. 查看 "Computed" 標籤
// 3. 找出哪個樣式最終生效
// 4. 查看是否被 style="" 覆蓋
```

#### 檢查 5：是否有其他 CSS 覆蓋？
```bash
# 檢查是否有其他檔案定義了相同的類別
grep -r "\.py-section-large" src/

# 如果有多個定義，後面的會覆蓋前面的
```

#### 檢查 6：Tailwind 是否正確生成？
```bash
# 檢查編譯後的 CSS（瀏覽器開發者工具 > Sources）
# 搜尋 .bg-black
# 確認 Tailwind 有生成這個類別
```

#### 檢查 7：值是否正確？
```css
/* 檢查變數值 */
:root {
  --spacing-section-large-desktop: 112px;  /* 是否正確？ */
}

/* 檢查類別定義 */
.py-section-large {
  padding-top: var(--spacing-section-large-mobile);  /* 變數名稱正確？ */
}
```

---

## 🎯 診斷決策樹

```
樣式不正確
    ↓
是否有該 CSS 類別？
    ├── 否 → 從 backup 提取並添加
    └── 是 ↓
        
        CSS 類別的值是否正確？
            ├── 否 → 對比 backup，修正值
            └── 是 ↓
                
                是否有內聯樣式？
                    ├── 是 → 內聯覆蓋了 CSS（內聯永遠贏）
                    │       └── 決定：保留內聯或改用 CSS
                    └── 否 ↓
                        
                        @layer 順序是否正確？
                            ├── 否 → 調整 @layer 順序
                            └── 是 ↓
                                
                                是否有其他 CSS 覆蓋？
                                    ├── 是 → 找出來源，調整
                                    └── 否 ↓
                                        
                                        Tailwind 是否生成該類別？
                                            ├── 否 → 檢查 Tailwind 配置
                                            └── 是 → 瀏覽器快取問題
                                                    └── 清除快取，硬重新整理
```

---

## 📝 快速參考

### 優先級記憶口訣
```
內聯最高覆蓋全
Utilities 強 Components 弱
Base 墊底做基礎
同級後者蓋前者
```

### 常見問題速查

#### Q1: 為什麼 .bg-black 不work？
```
檢查：
1. CSS 中有定義 .bg-black 嗎？
2. 是否有內聯 style={{ background }} 覆蓋？
3. 是否有其他 class 覆蓋（如 .bg-white 在後面）？
```

#### Q2: 為什麼 body 背景是白色而非透明？
```
檢查：
1. @layer base 中是否有 body { @apply bg-background; }
2. --background 變數值是什麼？
3. 是否應該移除全域背景設定？
```

#### Q3: 為什麼 Section padding 不正確？
```
檢查：
1. .py-section-large 是否定義？
2. 變數 --spacing-section-large-desktop 值正確？
3. 響應式 @media 斷點正確？
4. 是否在正確的 @layer utilities？
```

#### Q4: 為什麼動畫不work？
```
檢查：
1. @keyframes 是否定義？
2. animation 屬性是否正確引用？
3. 動畫名稱拼寫是否正確？
4. 是否有內聯 style={{ animation }} 覆蓋？
```

---

## 🔧 實用診斷命令

### 檢查 CSS 類別是否定義
```bash
grep "\.py-section-large" src/styles/utilities.css
grep "\.container-global" src/styles/utilities.css
```

### 檢查 CSS 變數是否定義
```bash
grep "spacing-section-large" src/styles/variables.css
grep "brand-500" src/styles/variables.css
```

### 檢查動畫是否定義
```bash
grep "@keyframes text-pulse" src/styles/animations.css
grep -c "@keyframes" src/styles/animations.css  # 總數
```

### 對比 backup 找差異
```bash
# 對比變數
diff <(grep "^\s*--" src/index.css.backup) <(grep "^\s*--" src/styles/variables.css)

# 對比動畫
diff <(grep "@keyframes" src/index.css.backup) <(grep "@keyframes" src/styles/animations.css)
```

---

## 🎯 關於內聯樣式的最終結論

### 何時保留內聯樣式

**✅ 必須保留：**
1. 動態值（props, state, Math.random()）
2. 複雜計算（多層漸層、複雜 transform）
3. JavaScript 控制的動畫
4. 運行時決定的樣式

**✅ 可以保留：**
1. 組件特定的獨特樣式
2. 一次性使用的樣式
3. 複雜到難以用 Tailwind 表達的樣式

### 何時移除內聯樣式

**⚠️ 應該改用 CSS：**
1. 靜態值（固定的顏色、尺寸）
2. 可重用的樣式
3. 與 Tailwind 類別重複的樣式
4. 團隊需要統一管理的樣式

### 處理策略

**階段 1：先重構 CSS（當前階段）**
```
目標：確保 CSS 100% 正確還原
方式：不管內聯，專注於 CSS
結果：CSS 定義完整且正確
```

**階段 2：再處理內聯衝突（之後）**
```
目標：優化內聯樣式
方式：
  1. 識別靜態內聯
  2. 評估是否與 CSS 衝突
  3. 決定保留或移除
結果：內聯只用於動態值
```

**優先順序：**
```
1. 先確保 CSS 正確 ⬅️ 當前階段
2. 再優化內聯樣式 ⬅️ 後續階段
```

---

## 📋 診斷檢查表

### 當樣式跑掉時，按順序檢查：

#### ✓ Level 1: CSS 定義檢查
- [ ] CSS 類別是否存在？
- [ ] CSS 變數是否定義？
- [ ] 值是否正確（對比 backup）？
- [ ] 拼寫是否正確？

#### ✓ Level 2: 優先級檢查
- [ ] @layer 順序是否正確？
- [ ] 是否有更高優先級的樣式覆蓋？
- [ ] 是否有內聯樣式覆蓋？

#### ✓ Level 3: Tailwind 生成檢查
- [ ] Tailwind 是否生成該類別？（瀏覽器檢查）
- [ ] PostCSS 配置是否正確？
- [ ] 編譯是否有錯誤？

#### ✓ Level 4: 瀏覽器檢查
- [ ] 清除瀏覽器快取
- [ ] 硬重新整理（Ctrl+Shift+R）
- [ ] 檢查 Computed 樣式
- [ ] 檢查是否有 JavaScript 動態修改

#### ✓ Level 5: 檔案載入檢查
- [ ] index.css 是否正確 import？
- [ ] 各分離檔案是否正確 @import？
- [ ] import 路徑是否正確？
- [ ] 檔案是否真的存在？

---

## 🚀 實戰案例

### 案例 1：Section padding 不正確

**症狀：**
```
Footer 的上下 padding 不對，應該有 80px，實際只有 48px
```

**診斷流程：**
```bash
# 1. 檢查 HTML
<footer className="py-section-medium">

# 2. 檢查 CSS 類別
grep "py-section-medium" src/styles/utilities.css
→ 找到定義 ✓

# 3. 檢查變數
grep "spacing-section-medium-desktop" src/styles/variables.css
→ --spacing-section-medium-desktop: 80px; ✓

# 4. 檢查類別定義
.py-section-medium {
  padding-top: var(--spacing-section-medium-mobile);   /* 48px */
  padding-bottom: var(--spacing-section-medium-mobile);
}

@media (min-width: 1280px) {
  .py-section-medium {
    padding-top: var(--spacing-section-medium-desktop);  /* 80px */
    padding-bottom: var(--spacing-section-medium-desktop);
  }
}

# 5. 檢查視窗寬度
瀏覽器寬度是否 >= 1280px？
→ 如果 < 1280px，顯示 48px 是正確的！✓
```

**結論：**
```
問題不在 CSS，是視窗寬度！
解決：調整視窗到 >= 1280px 或確認手機版 48px 是正確的
```

---

### 案例 2：Hero Section 背景錯誤

**症狀：**
```
Hero Section 應該是黑色背景 + 紫色漸層
實際：白色背景
```

**診斷流程：**
```bash
# 1. 檢查 HTML
<section className="bg-black" style={{ background: 'gradient...' }}>

# 2. 檢查內聯優先級
內聯 style 優先於 .bg-black
→ 應該顯示漸層，不是白色 ❌

# 3. 檢查是否有全域覆蓋
grep "body.*bg-" src/styles/variables.css

@layer base {
  body { @apply bg-background; }  ⬅️ 找到了！
}

# 4. 檢查變數值
--background: #ffffff;  ⬅️ 白色！

# 5. 分析優先級
body 的 bg-background（base 層）
vs
section 的 bg-black（utilities 層）
→ utilities 應該贏才對！

# 6. 檢查瀏覽器 Computed
發現：section 沒有 background 屬性！
→ 問題：.bg-black 沒有生成或被覆蓋

# 7. 檢查 Tailwind 生成
瀏覽器 > Sources > 搜尋 "bg-black"
→ 有找到定義 ✓

# 8. 最終發現
body 的 background: white 透過繼承影響了 section
內聯的 gradient 被某種原因覆蓋
```

**解決：**
```css
/* 移除 body 的全域背景 */
@layer base {
  body {
    /* background: white; */ ❌ 移除
    font-family: var(--font-sans); ✓
  }
}
```

---

## 📌 總結

### 核心原則

1. **內聯優先級最高（永遠）**
   - 無法用 CSS 覆蓋
   - 只能修改或移除內聯本身

2. **@layer 決定 class 優先級**
   - utilities > components > base
   - 用對 @layer 很重要

3. **編譯方式不影響優先級**
   - 預編譯 vs 動態生成 = 一樣
   - 重構不會改變優先級

4. **先修 CSS，後處理內聯**
   - 確保 CSS 100% 正確
   - 再決定內聯的去留

### 診斷順序
```
1. CSS 定義 → 2. 變數存在 → 3. @layer 順序 → 
4. 內聯覆蓋 → 5. 其他覆蓋 → 6. Tailwind 生成 → 
7. 瀏覽器快取
```

---

**診斷指南完成！** 🎯



