{
  "servers": [
    {
      "name": "Frontend (Vite)",
      "command": "npm run dev",
      "cwd": "frontend",
      "port": 3000,
      "description": "React frontend dev server with Vite + TailwindCSS"
    },
    {
      "name": "Backend (FastAPI)",
      "command": "uv run uvicorn app.main:app --reload --host 0.0.0.0 --port 8000",
      "cwd": "backend",
      "port": 8000,
      "description": "FastAPI backend API server with auto-reload"
    }
  ]
}
